# Minimal mosquitto config for the simulation stack.
# No bridge to a remote broker — sim is fully local. Anonymous access is fine
# because the broker is only reachable on localhost when network_mode: host
# is used and the listener is not exposed outside the developer's machine.

listener 1883
allow_anonymous true
